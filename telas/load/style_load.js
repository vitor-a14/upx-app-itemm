import { StyleSheet } from "react-native"

export const styleLoadScreen = StyleSheet.create({
    container: {
      flex: 1,
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: 'rgba(89, 123, 237, 193)'
    },
    image:{
        
    }
  });